<Window x:Class="WorkTimeCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:WorkTimeCalculator.ViewModels"
        Title="Work Time Calculator"
        Height="720"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackgroundBrush}"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="24" BorderBrush="{StaticResource NeutralMidBrush}" BorderThickness="0 0 0 1">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" Spacing="12">
                    <Button Style="{StaticResource OutlinedButtonStyle}" Command="{Binding ExportCommand}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <materialDesign:PackIcon Kind="FileExport" />
                            <TextBlock Text="Export" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <materialDesign:PackIcon Kind="ClockOutline" Width="40" Height="40" Foreground="{StaticResource PrimaryBrush}" />
                    <StackPanel>
                        <TextBlock Text="Work Time Calculator" Style="{StaticResource Headline1Text}" />
                        <TextBlock Text="Plan your day and leave right on time" Style="{StaticResource CaptionText}" />
                    </StackPanel>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="24">
            <TabControl ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}" Background="Transparent" BorderThickness="0">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border x:Name="HeaderBorder" Background="Transparent" Padding="12" Margin="0 0 8 0" CornerRadius="12">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <materialDesign:PackIcon x:Name="HeaderIcon" Kind="{Binding Icon}" Foreground="{StaticResource NeutralDarkBrush}" />
                                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource PrimaryLightBrush}" />
                                            <Setter TargetName="HeaderBorder" Property="materialDesign:RippleAssist.IsCentered" Value="True" />
                                            <Setter TargetName="HeaderIcon" Property="Foreground" Value="{StaticResource PrimaryBrush}" />
                                            <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding Content}" />
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>

        <Border Grid.Row="2" Background="White" Padding="16" BorderBrush="{StaticResource NeutralMidBrush}" BorderThickness="1 1 1 0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource CaptionText}" />
            </Grid>
        </Border>
    </Grid>
</Window>
