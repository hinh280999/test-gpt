<UserControl x:Class="WorkTimeCalculator.Views.TodayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:WorkTimeCalculator.Views.Controls"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="720">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0 0 24 0">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Quick calculate" Style="{StaticResource SectionTitleText}" />
                        <TextBlock Text="Plan the rest of your day with smart break insights." Style="{StaticResource CaptionText}" />

                        <Grid Margin="0 16 0 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Start time" Style="{StaticResource CaptionText}" />
                                <controls:ModernTimePicker Time="{Binding StartTime, Mode=TwoWay}" />
                                <StackPanel Orientation="Horizontal" Margin="0 12 0 0">
                                    <Button Content="Start Now" Command="{Binding StartNowCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                                    <Button Content="Standard Day" Command="{Binding StandardDayCommand}" Style="{StaticResource OutlinedButtonStyle}" />
                                    <Button Content="Flexible" Command="{Binding FlexibleDayCommand}" Style="{StaticResource OutlinedButtonStyle}" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="16 0 0 0">
                                <TextBlock Text="Break setup" Style="{StaticResource CaptionText}" />
                                <ListBox ItemsSource="{Binding BreakOptions}"
                                         SelectedItem="{Binding SelectedBreakOption, Mode=TwoWay}"
                                         Background="Transparent"
                                         BorderBrush="Transparent"
                                         Margin="0 8 0 0">
                                    <ListBox.ItemContainerStyle>
                                        <Style TargetType="ListBoxItem">
                                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                            <Setter Property="Padding" Value="0" />
                                            <Setter Property="Margin" Value="0 4 0 4" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ListBoxItem">
                                                        <Border x:Name="Container"
                                                                Background="{StaticResource NeutralLightBrush}"
                                                                CornerRadius="10"
                                                                Padding="12">
                                                            <ContentPresenter />
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Container" Property="Background" Value="{StaticResource PrimaryLightBrush}" />
                                                                <Setter Property="Foreground" Value="{StaticResource PrimaryDarkBrush}" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ListBox.ItemContainerStyle>
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Label}" />
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <StackPanel Orientation="Horizontal" Margin="0 8 0 0" Visibility="{Binding SelectedBreakOption.IsCustom, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <TextBlock Text="Custom break (min):" VerticalAlignment="Center" Margin="0 0 8 0" />
                                    <TextBox Width="80" Text="{Binding CustomBreakMinutes, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <Border Background="{StaticResource PrimaryLightBrush}" CornerRadius="16" Padding="20" Margin="0 24 0 0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Recommended end" Style="{StaticResource CaptionText}" />
                                    <TextBlock Text="{Binding RecommendedEndTime}" FontSize="44" FontWeight="Bold" Foreground="{StaticResource PrimaryDarkBrush}" />
                                    <TextBlock Text="{Binding CalculationBreakdown}" Style="{StaticResource BodyText}" />
                                    <TextBlock Text="{Binding CountdownText}" FontWeight="SemiBold" Margin="0 8 0 0" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <materialDesign:CircularProgressBar Value="{Binding ProgressValue}" Maximum="100" Width="96" Height="96" StrokeThickness="6" />
                                    <TextBlock Text="Progress" HorizontalAlignment="Center" Margin="0 8 0 0" />
                                    <ToggleButton Content="Notify me 15 mins before"
                                                  Style="{StaticResource OutlinedButtonStyle}"
                                                  IsChecked="{Binding NotificationsEnabled}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Log today's work" Style="{StaticResource SectionTitleText}" />
                        <Grid Margin="0 8 0 16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Start" Style="{StaticResource CaptionText}" />
                                <controls:ModernTimePicker Time="{Binding EntryStartTime, Mode=TwoWay}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="16 0 0 0">
                                <TextBlock Text="End" Style="{StaticResource CaptionText}" />
                                <controls:ModernTimePicker Time="{Binding EntryEndTime, Mode=TwoWay}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Margin="16 0 0 0" VerticalAlignment="Center">
                                <TextBlock Text="Break (min)" Style="{StaticResource CaptionText}" />
                                <Slider Width="140"
                                        Minimum="0"
                                        Maximum="180"
                                        TickFrequency="15"
                                        IsSnapToTickEnabled="True"
                                        Value="{Binding EntryBreakMinutes, Mode=TwoWay}" />
                                <TextBlock Text="{Binding EntryBreakMinutes, StringFormat='{}{0} min'}" HorizontalAlignment="Center" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
                            <ComboBox Width="180"
                                      ItemsSource="{Binding Categories}"
                                      DisplayMemberPath="Name"
                                      SelectedValuePath="Name"
                                      SelectedValue="{Binding SelectedCategory}" />
                            <TextBox Width="240"
                                     Margin="12 0 0 0"
                                     Text="{Binding EntryNotes}" materialDesign:HintAssist.Hint="Notes" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0 0 0 16">
                            <Button Content="Full Day (8h)" Command="{Binding ApplyTemplateCommand}" CommandParameter="Full" Style="{StaticResource OutlinedButtonStyle}" />
                            <Button Content="Half Day (4h)" Command="{Binding ApplyTemplateCommand}" CommandParameter="Half" Style="{StaticResource OutlinedButtonStyle}" />
                            <Button Content="Short Day (6h)" Command="{Binding ApplyTemplateCommand}" CommandParameter="Short" Style="{StaticResource OutlinedButtonStyle}" />
                        </StackPanel>

                        <Button Content="Add entry" Command="{Binding AddEntryCommand}" Style="{StaticResource PrimaryButtonStyle}" />
                        <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource CaptionText}" Foreground="{StaticResource WarningBrush}" />

                        <ItemsControl ItemsSource="{Binding TodayEntries}" Margin="0 24 0 0" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Weekly progress" Style="{StaticResource SectionTitleText}" />
                        <Grid Margin="0 8 0 0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <materialDesign:CircularProgressBar Value="{Binding WeeklyProgress}" Maximum="100" Width="120" Height="120" StrokeThickness="8" />
                            <StackPanel Grid.Column="1" Margin="16 0 0 0">
                                <TextBlock Text="{Binding WeeklyHours, StringFormat='{}{0:F1}h /'}" FontSize="20" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding WeeklyGoal, StringFormat='{}{0:F1}h goal'}" Style="{StaticResource CaptionText}" />
                                <TextBlock Text="{Binding WeeklyStatus}" Style="{StaticResource BodyText}" Margin="0 12 0 0" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Daily hours" Margin="0 16 0 8" Style="{StaticResource CaptionText}" />
                        <ItemsControl ItemsSource="{Binding RecentActivity}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0 4 0 4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Entry.Date, StringFormat='{}{0:ddd, MMM d}'}" />
                                        <ProgressBar Grid.Column="1"
                                                     Height="6"
                                                     Margin="12 12"
                                                     Minimum="0"
                                                     Maximum="12"
                                                     Value="{Binding Entry.Duration.TotalHours}" />
                                        <TextBlock Grid.Column="2" Text="{Binding Entry.Duration.TotalHours, StringFormat='{}{0:F1}h'}" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Monthly overview" Style="{StaticResource SectionTitleText}" />
                        <UniformGrid Columns="2" Margin="0 16 0 0">
                            <StackPanel>
                                <TextBlock Text="Total hours" Style="{StaticResource CaptionText}" />
                                <TextBlock Text="{Binding MonthlyHours, StringFormat='{}{0:F1}h'}" FontSize="24" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Average daily" Style="{StaticResource CaptionText}" />
                                <TextBlock Text="{Binding MonthlyAverage, StringFormat='{}{0:F1}h'}" FontSize="24" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Days worked" Style="{StaticResource CaptionText}" />
                                <TextBlock Text="{Binding DaysWorked}" FontSize="24" FontWeight="SemiBold" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Overtime" Style="{StaticResource CaptionText}" />
                                <TextBlock Text="{Binding OvertimeStatus}" FontSize="24" FontWeight="SemiBold" />
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
