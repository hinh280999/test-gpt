<UserControl x:Class="WorkTimeCalculator.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1100"
             d:DesignHeight="720">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="24">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource OutlinedButtonStyle}" />
            </StackPanel>

            <UniformGrid Columns="2" Margin="0 16 0 0" Rows="2" HorizontalAlignment="Stretch">
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Peak hours" Style="{StaticResource SectionTitleText}" />
                        <TextBlock Text="{Binding PeakHoursDescription}" FontSize="24" FontWeight="SemiBold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Break patterns" Style="{StaticResource SectionTitleText}" />
                        <TextBlock Text="{Binding BreakPatternDescription}" FontSize="24" FontWeight="SemiBold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardStyle}" Grid.ColumnSpan="2">
                    <StackPanel>
                        <TextBlock Text="Weekly comparison" Style="{StaticResource SectionTitleText}" />
                        <ItemsControl ItemsSource="{Binding WeeklyComparisons}" Margin="0 16 0 0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="32" Margin="4" Background="{StaticResource PrimaryLightBrush}" CornerRadius="6">
                                        <Border Height="{Binding}" Background="{StaticResource PrimaryBrush}" VerticalAlignment="Bottom" CornerRadius="6" />
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Border Style="{StaticResource CardStyle}" Margin="0 16 0 0">
                <StackPanel>
                    <TextBlock Text="Category distribution" Style="{StaticResource SectionTitleText}" />
                    <ItemsControl ItemsSource="{Binding CategoryDistribution}" Margin="0 16 0 0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0 8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Category}" />
                                    <ProgressBar Grid.Column="1" Value="{Binding Percentage}" Maximum="100" Height="6" Margin="12 8" />
                                    <TextBlock Grid.Column="2" Text="{Binding Percentage, StringFormat='{}{0:F1}%'}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource CaptionText}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
