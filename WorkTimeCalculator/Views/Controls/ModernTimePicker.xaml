<UserControl x:Class="WorkTimeCalculator.Views.Controls.ModernTimePicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignWidth="280"
             d:DesignHeight="64">
    <Border Background="White"
            CornerRadius="12"
            Padding="12"
            BorderBrush="{StaticResource NeutralMidBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox ItemsSource="{Binding Hours, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      SelectedItem="{Binding SelectedHour, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                      Style="{StaticResource MaterialDesignComboBox}"
                      materialDesign:HintAssist.Hint="Hour" />
            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding Minutes, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      SelectedItem="{Binding SelectedMinute, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                      Style="{StaticResource MaterialDesignComboBox}"
                      materialDesign:HintAssist.Hint="Minute" />
            <ToggleButton Grid.Column="2"
                          Style="{StaticResource SwitchToggleStyle}"
                          Margin="8 0 0 0"
                          IsChecked="{Binding IsPm, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}">
                <TextBlock Text="PM" Margin="56 0 0 0" VerticalAlignment="Center" />
            </ToggleButton>
        </Grid>
    </Border>
</UserControl>
